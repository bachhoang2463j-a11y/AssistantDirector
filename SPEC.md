# Assistant Director（副导演）项目设计规范文档

**版本：V0.2** ｜ 单文件形态：`酒馆助手脚本-副导演.json`（TavernHelper/JS-Slash-Runner **悬浮窗脚本插件**，AiRadio 同构——非正则替换的内联 iframe 页面）

> **V0.2 变更摘要**：① 废弃短标记（【战斗标记点】），开战宣告唯一通道改为最短 `<Combat_block>`；② 战斗修补/导演兜底人格全部移注 RpgCombat（V10.12），本插件删除该项；③ 态势模型重写——删除 18 场景模板/粗路由假设（AIRP 地点不可穷尽），改为**态势卡片池**（暗线位复审 + 态势位即时产卡双轨）；④ 触发定稿：暗线报告 newday 主节律 + 事件号外；⑤ 双端点定稿：暗线位（次高智力）+ 态势位（快速小模型）；⑥ 新增单 JSON 文件结构（§4.8）。

---

## 1. 项目目标与定位

副导演是 AIRP（AI 自由角色扮演）跑团的**世界模拟器**：让世界不等玩家行动也在运转。它与既有两件套构成完整生态：

| 插件 | 职责 | 一句话 |
| :--- | :--- | :--- |
| MiniMapStatus (MMS) | 状态维护（数值/物品/地图/名册） | "我是谁、我在哪、我有什么" |
| RpgCombat | 战斗执行 + 开战初始化（Combat_block 轮询/图鉴载入/导演兜底） | "打的时候怎么算" |
| **AssistantDirector** | 世界模拟（态势/暗线/派系/公开情报） | "世界在背后做什么" |

**要解决的三个痛点**：
1. 正文 LLM 顺从倾向严重——玩家调查几乎不可能失败（→ 叙事阻力注入）；
2. 上下文塞满后正文 LLM 遗忘世界书人物与伏笔（→ 事实提醒注入 + 公开情报流）；
3. 正文 AI 缺乏"世界在背后运转"的参照——敌人在哪、什么戒备、如何反应全靠即兴（→ 态势卡片注入）。

> **已移注边界**：开战初始化全链路（Combat_block 检测/解析/建局、未匹配敌名的导演兜底生成、图鉴写回）已由 RpgCombat V10.12 承担（其 README §10.7）。副导演属地为纯叙事：态势、暗线、派系、公开情报。本插件对战斗数值**零接触**。

---

## 2. 核心设计原则（六条，不可妥协）

1. **程序管数值，AI 管叙事（完全体）**：正文 AI 在整条数值链路上零接触——开战宣告是**点名册**（Combat_block 只写名字），我方数值走 MMS 直连、敌方数值走图鉴/导演兜底（均为 RpgCombat 属地）。
2. **双层信息架构**：玩家只看**公开征兆**（贴边栏：surface），正文 AI 收**幕后真相**（隐藏注入：truth + 渗透方式 + 禁泄清单）。两者成对生成、互不可见对方层，中间的推导距离就是调查空间。玩家无上帝视角选择权。
3. **模拟而非剧本**：不预知玩家行动、不枚举触发器。敌人放置的抽象是**驻防态**（谁/多少/什么戒备/怎么反应），遇敌是语义涌现（正文 AI 判定），不是动作映射。
4. **宣告而非生成**：开战 = 正文 AI 输出最短 `<Combat_block>`（我方/敌方各只写名字）；检测、匹配、展开、建局全是 RpgCombat 程序死活。
5. **开战链路不死锁**：态势注入是**增强层**而非前置条件——未命中卡片池的地点没有菜单也能开战（正文 AI 从图鉴选名 + RpgCombat 兜底链接住）。
6. **地点不可穷尽**（V0.2 硬约束）：AIRP 中地点自由涌现（"悉尼·某后巷·某个苦力的棚屋"），任何依赖预编译地点表、穷举路由的设计不成立。**态势内容的本质是语义判断，归 LLM**（暗线位报告复审 / 态势位即时产卡）；程序只做匹配、配发、校验与触发。MMS 的 18 个 SVG 地图是纯视觉模板，无派系/戒备语义，本插件不引用其作为知识库。

---

## 3. 架构总览

```
┌─ 呈现层（纯程序）──────── 贴边折叠栏悬浮窗：折叠态电报纸带 ticker / 展开态派系情报卡
│                         surface → 玩家 + 单向流向 MMS 行动选项
├─ 态势配发层（每楼，纯程序）─ 地点文本匹配卡片池 → 命中：拼态势注入（驻防+菜单+判定标准）
│                         未命中：通用兜底注入 + 待登记队列
│                         预约条件每楼检查 → 命中引爆（戒备置顶 + 主动接触态标注）
├─ 态势位（随地点，快速小模型）─ 新地点即时产初判卡（分兵时一次多卡）；当日由报告轨复审收编
└─ 战略层（暗线位·次高智力，天级+事件号外）
                          暗线人格：派系动态/叙事阻力/预约埋雷 + 态势卡片池全量复审
                          报告存档（聊天变量+GM 面板）→ 三路分发（surface 卡 / truth 注入 / 卡片池）
```

**数据流闭环**：
```
正文 AI（最强模型）每楼输出：正文 + 状态栏标记点（MMS 属地）+ 条件成立时最短 <Combat_block>
程序（副导演）每楼读 stat_data：地点变更→卡片匹配/即时产卡；日期跨日/事件→暗线报告
程序（RpgCombat）轮询 Combat_block → 图鉴载入/导演兜底 → 建局 → 本地结算
$rpg_combat_result（RpgCombat 程序写 MMS）→ 副导演监听 → 事件号外（暗线推进+戒备调整）
```

**可见性矩阵**（双层架构的精确边界）：

| 内容 | 玩家 | 正文 AI |
| :--- | :--- | :--- |
| `factions.surface`（公开征兆） | ✅ 贴边栏情报卡 + ticker | ❌ 上下文中不存在 |
| `factions.truth` + `resistance` | ❌（调查目标） | ✅ 暗线注入（三态标注+渗透方式+禁泄） |
| `garrisons` 卡片（两轨产出同待遇） | ❌（经正文演出间接感知） | ✅ 态势注入（**仅当前地点那张**） |
| `ambush预约`（未引爆） | ❌ | ❌ 引爆前双方皆盲；引爆后才经态势注入带出 |
| `stage` | ✅ 报头阶段标签 | ✅ 事实提醒一句 |
| 全量报告 JSON | GM 面板（主动审计） | ❌ 永不进上下文 |

正文 AI 的上下文里只有**两条**副导演注入（态势 + 暗线，各一个深度0 system 隐藏块，替换式）；玩家的屏幕上只有一份报纸（surface）。

---

## 4. 协议标准

### 4.1 开战宣告通道（`<Combat_block>` 唯一通道）

> 历史注记：短标记 `【战斗标记点丨我方:…丨敌方:…】` 因与状态栏正则/酒馆 markdown 管线冲突过多已废弃（随 RpgCombat 功能回退）。开战宣告唯一通道为 `<Combat_block>`，由 RpgCombat `startSTPolling` 惰性正则检测。

**最短格式**（正文 AI 的全部开战义务 = 一份点名册）：

```yaml
<Combat_block>
战斗数据栏:
  我方列表:
    - 角色:
        名字: "埃利奥特"
    - 角色:
        名字: "弗兰克"
  敌方列表:
    - 角色:
        名字: "伊塔库亚的眷族"
</Combat_block>
```

| 数值环节 | 提供方 | 说明 |
| :--- | :--- | :--- |
| 我方属性/弹药/消耗品 | MMS `stat_data` | RpgCombat V10.11「状态栏传入」直连覆盖 |
| 我方技能 | RpgCombat 持久化 | 聊天变量中玩家点保存的角色数据 |
| 敌方数值 | 图鉴词条 / 导演兜底 | RpgCombat V10.12 世界书载入 + 兜底生成链 |
| 正文 AI | **只写名字** | 可用增强：`*N` 数量后缀、`[变种]` 标签（RpgCombat 原生语义） |

**战斗轮规则**：世界书前部常驻一段简短规则（格式教学/我方名单处理），由用户维护——单一事实源。副导演态势注入**不重复规则全文**，只带提醒（见 §4.5）。

### 4.2 态势卡片模型

**态势卡片 = 语义驻防描述**（谁/多少/什么戒备/怎么反应）+ 可选敌方菜单。卡片是数据，不是模板查询——它由 LLM 按剧情现写。

```jsonc
{ "place": "绿顶酒馆", "aliases": ["凯特的酒馆", "Green Roof"],
  "faction": "凯特·利（萨里山）",
  "menu": "萨里山剃刀党混混*4~6/私酒蒸馏师*3~5/黑帮职业杀手*1~2",
  "alert": "警戒",            // 松懈/常规/警戒/严密
  "reaction": "应邀客人以礼相待、敬酒试探；亮械或闯后场酒窖立即翻脸；对便衣高度敏感",
  "source": "daily|instant" } // 报告轨复审 / 态势位即时初判
```

**双轨生产**：

| | 报告轨（暗线位） | 即时轨（态势位） |
| :--- | :--- | :--- |
| 触发 | 每份报告时（newday/号外/兜底/手动） | 地点变更且卡片池未命中时 |
| 范围 | 全量复审：修正既有卡 alert/reaction/menu、增补预判卡、收编当日即时卡 | 仅新地点初判卡（分兵时一次多卡，点位从 stat_data 不在场角色描述收集） |
| 输入 | 报告全量输入（§4.3） | 新地点名 + 该楼正文尾部 ~600 字 + 图鉴词条名单（名字/等级/派系）+ 名册 + 当前阶段 |
| 输出 | 完整报告（含 garrisons 数组） | 1~N 张卡片 JSON |
| 时效 | 报告级 | 楼级（产卡在楼层渲染后异步，下一楼生效，不阻塞正文） |

**每楼配发流程（纯程序，零 LLM）**：
1. 读最新 `stat_data`（`getVariables({type:'message', message_id:-1})` 自动回溯最近快照）；
2. 地点文本 → 卡片池**三级匹配**（剥 emoji 精确 → aliases → 双向包含，复用 RpgCombat 图鉴匹配语义）；
3. **命中**：拉卡片 → menu 数量区间按 stat_data 规模公式取值 → 拼态势注入（替换式）；
4. **未命中**：拼通用兜底注入 + 该地点入待登记队列（下次即时轨产卡；报告轨兜底收编）；
5. 预约条件检查（结构化：时间区间 + 地点∈集合）→ 命中即引爆（对应卡 alert 置顶 + 注入带主动接触态标注）。

**规模随行重算**（保留的程序性公式）：每楼按最新 stat_data（HP/弹药/倒地）在卡片数量区间内取值——我方有倒地或弹药过半 → 数量 -1~2 或降一档。参考基准：普通=1级×4~6 或 2级×2~3；硬仗=3级×2+2级×2；头目=4级×1+2~3级随从；终局=5级×1+精英+杂兵。

**卡片硬校验**（程序侧，产出时校验而非注入时）：`menu` 每个敌方名必须 ∈ 图鉴 V3.0 词条名单（白名单 JSON）；`faction` 必须在名册；校验失败带错误反馈重试 ≤1，再失败即丢弃该卡（即时轨回落通用注入，报告轨丢弃该条不阻塞其余产出）。

### 4.3 暗线报告（战略层输出 schema）

```jsonc
{
  "stage": "阶段判断 + 依据楼层",
  "factions": [                       // 派系动态（surface/truth 成对）
    { "name": "凯特·利（萨里山）", "surface": "公开征兆（进贴边栏）",
      "truth": "幕后真相（仅进暗线注入）", "causes": ["楼23 埃利奥特心理推演"],
      "state": "推断中|已渗透|已兑现" }
  ],
  "resistance": {                     // 叙事阻力（治顺从性）
    "forbidden": [ { "truth": "禁泄真相", "path": "正确获取途径", "leak_cost": "过早泄露毁掉什么" } ],
    "partial": [ "强行调查应得的部分信息/误导" ],
    "friction": [ "来自已登场势力动机的环境阻力" ]
  },
  "roster_ops": [ "新派系提案（轻量登场：仅征兆级）" ],
  "garrisons": [ /* 态势卡片数组，见 §4.2 */ ],
  "ambush预约": [ { "派系": "…", "条件": { "时间": "6/13夜-6/14凌晨", "地点∈": ["达令港仓库区","货运车队沿线"] },
                    "规模": "…", "引爆态": "严密" } ]   // 条件必须结构化可程序检查
}
```

**触发矩阵**（暗线报告的节律 = 世界时钟；每楼零 LLM 调用）：

| 类型 | 触发条件 | 语义 |
| :--- | :--- | :--- |
| 主节律 | stat_data 日期字段跨日（newday） | 晨报：推演昨夜动向 + 今日预约检查 + 卡片全量复审 |
| 号外 | `$rpg_combat_result` 战斗结算 | 事件快报（暗线推进 + 戒备调整） |
| 号外 | 灰瘟/剧情阶段字段变更 | 阶段剧变，不等天亮 |
| 号外 | 跨城市级地点迁移 | 换战略棋盘 |
| 兜底 | 同一游戏日内 ≥15~20 楼无任何报告 | 强制增量报告，防暗线与正文脱节 |
| 手动 | GM 面板「立即推演」按钮 | 排障 + 玩家主动 |

触发去抖：swipe 会使 stat_data 来回跳，只在**新楼生成后**检查、与上次报告时点比对。

**报告输入组装**（输入越短出处越准）：LWB storySummary 结构化摘要（复用既有基础设施，自带事件楼层号）+ 最近 2~3 轮原文（剥状态栏/Combat_block）+ 分层标注 stat_data（硬事实）+ 名册（含墓碑）+ 待登记地点清单 + 当前卡片池。

**硬校验**（程序侧，不过即丢弃该条/重试）：
- `causes` 必填且必须引用真实楼层号——"已发生"的判定就是有无出处；
- `truth` 与 `surface` 成对——孤立的真相或征兆都是废条目；
- 三态标记必填；仅"已兑现"可直接陈述，"推断中"注入时必须带"仅可环境渗透，禁止直接揭示"标注；
- 派系名必须在名册（含墓碑检查——墓碑内派系禁止复活）；
- `garrisons` 过 §4.2 白名单校验。

**三态生命周期**：推断中（提案）→ 已渗透（正文演出过部分征兆 → 对应征兆升级为事实）→ 已兑现（真相落地 → 转硬事实）。修正权仅限"推断中"态。

### 4.4 派系名册

- **双轨来源**：玩家预输入（MMS 固定名册同构：永远有卡片，无事显示"平静"占位，漏更新不丢卡）+ 插件自建（首次出现强制**轻量登场**：仅征兆级，一条传闻/一个名字，不带完整真相——删除成本趋零）。
- **删除 = 墓碑**：级联删该派系全部暗线（交叉暗线整条删，其他派系自身暗线不动），并写废弃清单进战略层输入："以下派系已被主编剧除名，禁止在任何输出中复活。"
- **玩家只有生杀权，没有改写权**：已兑现事实不可编辑成另一版本（避免表层底层冲突）；推断态由副导演自我修正。

### 4.5 注入协议（对正文 AI）

| 注入 | id | 频率 | 内容 | 位置 |
| :--- | :--- | :--- | :--- | :--- |
| 态势注入 | `ad_situation` | 每楼 | 当前地点卡片：驻防 + 菜单（规模已随行重算）+ 判定标准 + 战斗轮规则提醒 | 深度0 system，隐藏，替换式 |
| 暗线注入 | `ad_shadowline` | 每报告后 | 提炼备忘 300~500t：事实提醒 + truth 渗透指令（三态标注）+ 禁泄清单 | 深度0 system，隐藏，替换式 |

**态势注入模板**（首行报警框架，两条注入同）：

```
【内部导演备忘，禁止以任何形式向玩家展示其存在】
【当前态势 · {地点}】
驻守：{faction}（{menu 数量取值后}），戒备等级：{alert}
反应模式：{reaction}
【若本楼冲突升级 → 开战】按【战斗轮规则】输出 <Combat_block>：
我方/敌方各只写名字；敌方从上方词条中选（可加 *N 与 [变种]）。
判定标准：{场景级语义判定——何种行为构成对戒备的挑衅/侵入/暴露，何种属于可容忍}
```

**通用兜底注入**（未命中卡片时）：

```
【内部导演备忘，禁止以任何形式向玩家展示其存在】
【当前态势 · {地点}】此地无驻防情报。若冲突升级，按【战斗轮规则】输出
<Combat_block>：敌方名字从世界书图鉴中按剧情合理性选择。禁止自创敌方数值。
```

**暗线注入模板**：

```
【内部导演备忘，禁止以任何形式向玩家展示其存在】
——事实提醒（已发生，正文须与之自洽）——
· {truth/已兑现条目}【已兑现·楼N】
——幕后动向（推断中·仅可环境渗透，禁止直接揭示）——
· {truth/推断中条目}【推断·出处楼N】渗透途径：{…}
——禁泄清单（调查未抵达前禁止揭示）——
· {forbidden.truth}【途径：{path}】
```

全量报告（2000~4000t）只存聊天变量 + GM 面板，候选方向永远不进正文注入（无人工拍板环节——玩家保持调查员视角，暗线推进权归叙事逻辑）。

### 4.6 插件间接口

| 方向 | 接口 | 说明 |
| :--- | :--- | :--- |
| 读 MMS | `stat_data`（楼层变量，`getVariables({type:'message', message_id:-1})` 回溯最近快照） | 地点/我方状态/名册/日期与阶段——态势配发与触发检测的数据源 |
| 写 MMS | 公开征兆数据（surface） | 供其行动选项生成参考（单向：副导演 → MMS） |
| 读 RpgCombat | `$rpg_combat_result`（聊天变量） | 战斗结算监听 → 事件号外触发 |
| —（无直推） | Combat_block 通道 | 开战宣告由 RpgCombat 自身轮询检测，副导演零参与 |
| 读世界书 | 敌方图鉴 V3.0（41 词条） | 态势位输入的词条名单 + 卡片 menu 白名单校验源 |
| 复用 LWB | storySummary 结构化摘要 | 报告输入压缩（自带事件楼层号出处） |

### 4.7 双模型端点配置

| 模型位 | 用途 | 要求 |
| :--- | :--- | :--- |
| 暗线位 | 战略层报告（天级+事件） | **次高智力**、长上下文、低温（0.3~0.5）、结构遵循优先 |
| 态势位 | 即时产卡（随地点，均 ~3 楼一次） | 快速小模型、低温、JSON 输出稳定 |

设置面板沿用 AiRadio `custom_api` 模式（Base URL / Key / Model / 参数），两套独立存储与持久化（localStorage，AiRadio 同构）。

### 4.8 单 JSON 文件结构（酒馆助手悬浮窗脚本插件）

**外壳**（AiRadio 同构）：

```json
{
  "type": "script",
  "enabled": false,
  "name": "Assistant Director (副导演·世界模拟器)",
  "id": "<uuid>",
  "content": "<UserScript 主体（全部逻辑）>",
  "info": "AIRP 世界模拟器：暗线推演/态势卡片/公开情报贴边栏",
  "button": { "enabled": true, "buttons": [] },
  "data": {},
  "export_with": { "data": true, "button": true }
}
```

**`content`（UserScript）内部分区**（模块化组织，预计 3000~5000 行）：

```
// ==UserScript==（@name/@version/@description/@grant none，AiRadio 头格式）
// ═══ 0. 常量与配置 ═══  版本号 / 默认设置 / 注入模板 / 报警框架首行 / 固定注入 id
// ═══ 1. 设置管理 ═══    localStorage 读写 / 双端点配置（AiRadio custom_api 面板模式）
// ═══ 2. 酒馆环境适配 ═══ 环境检测（脚本环境直用全局 API；降级路径走 window.parent）
//                        API 封装：injectPrompts/uninjectPrompts（替换式注入）
//                        getVariables（message:-1 读 stat_data）/ insertOrAssignVariables（chat 写 $ad_*）
//                        eventOn（tavern_events.*）/ getChatMessages / 世界书读取
// ═══ 3. 楼层监听状态机 ═══ MESSAGE_RECEIVED/UPDATED/SWIPED/CHAT_CHANGED
//                        swipe 去抖 / 跨日检测→报告触发 / 地点变更检测→产卡触发
//                        $rpg_combat_result 监听→号外 / 阶段字段变更→号外
// ═══ 4. 态势卡片引擎 ═══  卡片池 CRUD / 地点三级匹配 / 规模随行重算 / 注入拼装
//                        通用兜底 / 待登记队列 / 预约条件检查与引爆 / menu 白名单校验
// ═══ 5. LLM 客户端 ═══   双端点 fetch（/chat/completions 非流式）/ JSON 提取容错 / 重试
// ═══ 6. 战略层（暗线人格）═══ 输入组装（LWB 摘要+近3楼原文+分层 stat_data+名册+墓碑+待登记）
//                        schema 硬校验（causes/truth-surface 成对/三态/白名单）
//                        报告存档 $ad_report / 提炼注入生成 / 三路分发
// ═══ 7. 态势位（即时产卡）═══ 输入组装（地点+正文尾部+图鉴名单+名册+阶段）
//                        单卡/多卡生成 / 校验 / 失败回落通用注入
// ═══ 8. 公开层 UI ═══    贴边折叠栏悬浮窗（document.body.appendChild + position:fixed）
//                        折叠 ticker / 展开报纸面板 / 派系卡 / 灰卡解锁 / 未读脉冲
//                        surface 单向输出 MMS
// ═══ 9. GM 面板 ═══      报告存档查看 / 手动「立即推演」/ 名册 CRUD / 墓碑
//                        注入审计（每条 truth 回溯 causes 楼层）
// ═══ 10. 主流程编排 ═══   初始化序列 / 事件接线 / 全局容错兜底
```

**存储分层**：

| 数据 | 存储 | 说明 |
| :--- | :--- | :--- |
| 端点/参数/UI 偏好 | localStorage（`ad_*` 键） | AiRadio 模式，随浏览器走 |
| 卡片池 | 聊天变量 `$ad_cards` | `$` 前缀对 LLM 隐形，随聊天文件走 |
| 最新报告 | 聊天变量 `$ad_report` | GM 面板与分发源 |
| 名册 + 墓碑 | 聊天变量 `$ad_roster` | 双轨 CRUD |
| 待登记 + 预约 | 聊天变量 `$ad_pending` | 即时轨/引爆消费 |
| 注入 | 固定 id `ad_situation` / `ad_shadowline` | 替换式：先 uninject 旧 id 再 inject 新内容 |

---

## 5. 功能边界

**In Scope**：
- 单 JSON 悬浮窗插件（外壳 + UserScript 全逻辑）
- 贴边折叠栏 UI（折叠 ticker / 派系情报卡 / 阶段揭示 / 未读脉冲）
- 态势卡片引擎（卡片池/三级匹配/规模重算/通用兜底/待登记闭环）
- 态势位即时产卡（随地点触发、分兵多卡、校验回落）
- 暗线人格（触发矩阵、schema 校验、三态生命周期、提炼注入、报告存档）
- 派系名册（双轨、墓碑、轻量登场）
- 预约-引爆（结构化条件每楼检查）
- GM 面板（报告查看/手动推演/名册管理/注入审计）

**Out of Scope**：
- 不做玩家上帝视角的选择面板（GM 拍板已被否决——调查员视角纯粹性）
- 不做数值修改/战斗结算/开战建局/导演兜底（全部 RpgCombat 属地）
- 不做状态维护（MMS 属地）
- 不依赖固定触发词表或预编译地点表（剧本思维与穷尽假设均被否决）
- 不做正则替换的内联楼层组件（悬浮窗形态，不与同楼层组件共享文本）

---

## 6. 幻觉防线（四道闸，按强度排序）

1. **出处强制**：`causes` 必填真实楼层引用，无出处条目不允许进真相层（程序校验）；
2. **三态隔离**：推断永远标"推断"，错不污染事实层；只有正文实际演出过才升级；
3. **输入压缩 + 白名单**：报告输入 = LWB 摘要 + 最近 2~3 轮原文 + 分层标注 stat_data（硬事实）；态势 menu 强制图鉴白名单——输入越短出处越准，菜单越窄幻觉越少；
4. 温度 0.3~0.5（辅助）。

---

## 7. UI 视觉规范

视觉 demo 已完成：`demo_story_director.html`（项目根目录，零依赖）。悬浮窗实现为 `document.body.appendChild` + `position:fixed` 贴边（AiRadio 同构），非楼层内 iframe。

- **折叠态**：右侧贴边 40px 竖条，竖排"世界情报 ✦ GAZETTE"标签 + 竖向滚动未读头条（电报机纸带隐喻）+ 未读琥珀金脉冲点；无动态时静默——"是否留意时事"是玩家决策。
- **展开态**：右缘滑出 372px 报纸增刊面板：报头（日期+剧情阶段）→ 派系情报卡×N（每条情报=一句话动态+来源+日期）→ 底注"本报仅刊载街头可见之事 · 真相须由读者自行抵达"。
- **派系卡**：预输入派系永远有卡（平静占位）；插件新建打"新面孔"标记；未接触派系灰态"？？？"（阶段揭示节奏器）；阶段推进时灰卡解锁带金色揭幕闪动。
- **视觉基因**：MMS slate 深底（#0f172a）+ 浅灰蓝文字（#cbd5e1）+ 琥珀金（#fbbf24）+ Courier 打字机字体 + 1920s 报纸双线框/竖分栏线；与 `regex-1920s新一天新闻与账单美化.json` 美学同源。

---

## 8. 分步实现计划（每步可测）

| Step | 内容 | 验收测试 |
| :--- | :--- | :--- |
| **S0 骨架** | 单 JSON 外壳 + 悬浮窗贴边折叠栏 UI（自 demo 移植）+ 设置面板（双端点，localStorage 持久化） | IAB：折叠/展开缓动、ticker 循环、设置持久化；`node --check` 校验提取的 content |
| **S1 卡片引擎** | 卡片数据结构 + 每楼地点三级匹配 + 规模随行重算 + 态势注入拼装（injectPrompts 替换式）+ 通用兜底 | 手工填卡 → IAB 换地点楼层 → 注入内容变化断言；弹药减半 → 规模降档断言 |
| **S2 态势位** | 即时产卡全链路：未命中触发 → 输入组装 → 生成 → 白名单校验 → 失败回落 | 闯入未登记地点 → 下一楼注入含新卡；构造非法 menu → 断言被拒并回落 |
| **S3 战略层** | 触发矩阵（跨日/号外/兜底/手动）+ 报告输入组装 + schema 硬校验 + 报告存档 + 提炼注入 | mock 跨日 → 报告过校验 → 注入含三态标注；构造无 causes 条目 → 断言被拒 |
| **S4 公开层** | surface → 派系卡渲染（阶段揭示/新面孔/灰卡解锁）+ 事件号外刷新 + 单向输出 MMS | 报告生成后贴边栏卡片更新；阶段推进解锁灰卡；删除派系墓碑生效（不再复活） |
| **S5 名册与预约** | 名册 CRUD（双轨/墓碑/轻量登场）+ 预约条件每楼检查与引爆 | 埋预约 → 条件满足楼断言注入含主动接触态；墓碑派系在报告输出中断言为零 |
| **S6 集成回归** | 三插件全链路（真模型正文打最短 Combat_block → RpgCombat 建局 → 战斗 → 结算 → 号外）+ harness（参照 RpgCombat integration-test 模式，mock 酒馆环境） | 端到端断言套件全绿；连续 50 楼监听无泄漏、注入不堆积（替换式验证） |

**阶段分组**：S0–S1 = 配发就绪（纯程序，可手工喂卡运行）；S2–S3 = 双模型上线；S4–S5 = 完全体；S6 = 生态闭环。

---

## 9. 验收与质量标准

1. **兼容性**：单 JSON 在 Chrome/Edge 酒馆助手环境零控制台报错；悬浮窗降级条件（页面不可见/失焦）下逻辑兜底正常（IAB 验证）。
2. **数值零幻觉**：正文 AI 的任何输出不直接进入数值链路；副导演对战斗数值零接触；态势 menu 全程白名单可追溯（日志）。
3. **忠实性**：暗线注入的每条真相可回溯到 causes 楼层出处（GM 面板审计接口）。
4. **稳定度**：连续 50 楼监听无内存泄漏、注入不堆积（固定 id 替换式验证）；swipe 不误触发报告/产卡。
5. **成本纪律**：每楼零 LLM 调用；态势位平均 ≤1 次/3 楼；暗线位天级 + 号外。
6. **开发流程**：遵循 sillytavern-plugin-dev 分流——编码后提取 content 做 `node --check` → IAB mock 测试 → 用户导入真环境确认；布局动画类最终过真 Chrome（排障模式）。
