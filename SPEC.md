# Assistant Director（副导演）项目设计规范文档

**版本：V0.3.0** ｜ 单文件形态：`酒馆助手脚本-副导演.json`（TavernHelper/JS-Slash-Runner **悬浮窗脚本插件**，AiRadio 同构——非正则替换的内联 iframe 页面）

> **V0.2 变更摘要**：① 废弃短标记（【战斗标记点】），开战宣告唯一通道改为最短 `<Combat_block>`；② 战斗修补/导演兜底人格全部移注 RpgCombat（V10.12），本插件删除该项；③ 态势模型重写——删除 18 场景模板/粗路由假设（AIRP 地点不可穷尽），改为**态势卡片池**（暗线位复审 + 态势位即时产卡双轨）；④ 触发定稿：暗线报告 newday 主节律 + 事件号外；⑤ 双端点定稿：暗线位（次高智力）+ 态势位（快速小模型）；⑥ 新增单 JSON 文件结构（§4.8）。
> **V0.2.1 变更摘要**：① 注入协议语义修正——两条注入**持续在场**（常驻深度0，频率仅指内容刷新节奏）；② 新增绝对安全地点的态势注入变体（无可见敌人 + 保留认知外突袭）；③ LWB storySummary 输入接口落实（`SillyTavern.chatMetadata` 读取通路、增量窗口、与 LWB 注入的分工边界）。
> **V0.2.2 变更摘要**：① **信息完整性优先**（用户原则：宁可上下文塞满，不可缺信息让模型瞎编）——世界书同步词条全量注入（废除 800/4000 字截断）、产卡正文上下文全量（废除 600 字截断）；② 世界书同步机制：用户自选 世界书→词条，内容注入态势位/暗线位输入（图鉴勾"总览与索引"，剧情世界书勾派系/背景词条）；③ 图鉴校验容错升级（词条名带等级前缀，按 keys/剥前缀/包含四级匹配，命中规范化写回完整词条名）。
> **V0.2.3 变更摘要**：① 世界书同步拆**两套独立配置**（态势位产卡 / 暗线位报告各自选书勾词条，旧单一配置迁移复制到两套）；② 新增**副导演楼层窗口**设置：默认 20 楼 AI 原文（对齐 LWB 总结窗口；排除玩家输入与隐藏楼层；0=全部历史），每楼带楼层号；③ 副导演早期历史由 LWB 结构化总结覆盖（视野与正文一致：N 楼原文 + LWB 总结）。
> **V0.2.4 变更摘要**：**S3 暗线位战略层落地**——触发矩阵（newday/阶段变更/跨城市/战斗结算号外/同日15楼兜底/手动，busy 锁防并发）、报告生成（暗线位 LLM，全量五源输入）、schema 硬校验（causes 楼层出处/truth-surface 成对/三态/墓碑/garrisons 白名单）、三路分发（garrisons 收编卡片池复审 + 提炼注入 ad_shadowline 持续在场 + 存档 $ad_report）、名册自动注册（$ad_roster）、预约存档与引爆（$ad_pending，时间+地点命中→戒备置顶+主动接触态注入，卡片别名对齐地点匹配）、GM 📡 推演按钮与报告查看弹窗。
> **V0.2.6 变更摘要**：**注入通道迁移世界书词条（MMS 同构）**——`ad_situation`/`ad_shadowline` 两条注入从 `injectPrompts` 深度0 通道改为角色卡主世界书 constant 蓝灯词条（`副导演-态势`/`副导演-暗线`，at_depth/system/深度0/排序15），完全替代旧通道；位置仅首次创建时指定，脚本更新只改 content 不动 position——用户可在世界书编辑器自由调整顺序；**用户改动兜底（merge3）**：用户手动改过词条内容时，下一轮写入走三方行级合并（用户改/删/增的行持续保留、双改同行用户赢、脚本删行照删），注入始终包含用户修订；重挂载/换聊天按存档重建词条（无数据禁用不删除）——顺带修复重载页面后注入丢失的问题；④ **取消 surface 单向写回 MMS 计划**——不给状态栏 LLM 增加额外负担（玩家可不选择行动选项，自由根据已知线索行动），surface 只上报纸，§3/§4.6/§4.8/S4 计划同步移除该接口；⑤ 暗线输入补**可选敌人名单**（ambush 预约"规模"的选用源，与态势位产卡同一名单——引爆发敌与战斗链路对齐，铁律 10 同步指向）。
> **V0.2.7 变更摘要**：① **注入模板 XML 化**——两条注入（态势三形态 + 暗线五段）改为 XML 标签分段 + 两空格缩进 `- ` 列表（`<当前态势>`/`<内部导演备忘>` 包裹、段落各占一对标签），提升正文 LLM 注意力分区与人工可维护性；主动接触态插入 `</当前态势>` 之前保持包裹完整。② **LLM 调试日志渲染**——messages 按角色分块（system/user 徽标）+ `pre-wrap` 真实换行（废除 JSON.stringify 转义的字面量 `\n`）+ 记录补真实时间戳。③ **总开关取消，改双独立功能开关**——`enabledShadowline`（报告推演+暗线注入）与 `enabledSituation`（态势注入+即时产卡+预约引爆）互不牵连，切换即时生效（关→词条下灯；开→强写重新上灯），旧设置迁移。
> **V0.2.8 变更摘要**：**S5 名册 CRUD 落地**——GM 📜 名册弹窗（轻量登场入册 / 除名=墓碑 / 墓碑恢复）；除名级联清暗线（报告条目 + 提及该派的阻力/预约整条删 + 暗线词条即时重写 + surface 同步）；墓碑双重保险（提示词约束为主 + validateReport 程序过滤兜底 + 自动注册跳过墓碑）——验收"墓碑派系在报告输出中断言为零"。预约-引爆（S5 另一半）已于 V0.2.4 落地。
> **V0.2.9 变更摘要**：**S4 公开层完全体落地（灰卡阶段揭示）**——**接触即揭**：派系名出现在玩家可见视野（楼层原文/LWB/敌人名单）即署名（程序侧判定，不依赖 LLM 输出字段），`revealed` 单调锁防回退；未接触派系灰卡"？？？"遮名（征兆可见、不出占位卡不剧透）；插件自建"新面孔"标记（预输入无）；新揭示首拍金色揭幕闪动；已接触无条目派系"平静"占位卡；报告生成后报纸即时刷新（事件号外）；墓碑除名报纸即刻全清。GM 报头按钮移至报头顶部专属条（不再遮挡报头标题）。
> **V0.3.0 变更摘要**：**S7 世界引擎化重构（仿世界引擎设计）**——① **双位合并**：态势位/暗线位双 LLM 端点合并为单一"副导演 API"，双开关并为 `enabledDirector`，双 worldSync 并集迁移，双提示词预设并为 `DirectorPrompt`；② **世界状态 `$ad_world`**（取代 $ad_report）：factions（暗线三态全保留 + stance/relations/zone/morale 关系四字段）+ **events 事件链**（萌芽/发酵/逼近/爆发/平息 × stageRound 1-9）+ **winds 风声**（私下/流传/公开 × quietRounds 衰减）+ **encounter 遇敌概率档案**；③ **本地骰（每楼零 LLM，仿世界引擎）**：事件链阈值骰推进/受挫/晋级（爆发阶段注入"⚠ 临近冲突"提醒行，取代预约引爆）、风声安静超时按递增概率消散、`lastDiceFloorId` 幂等守卫（swipe 不重复推进）；④ **遇敌概率算法**：`spots（地标级）> districts（大区级）> 玩家设置` 三级兜底 + `clamp(-40,40, heat + eventTension)` 冷热加法修正（heat 为副导演宏观判断、eventTension 为本地点 conflict 事件阶段分微观重算），chance=0 显式安全区；⑤ **S6 接动态概率 + 防连战锁**（每推演周期最多一场随机战斗，推演成功解锁）；⑥ **触发节奏**：每 `directorEveryX`（默认3）楼心跳 + 战斗结果/跨日/阶段变化强制推 + 手动 📡；⑦ **拆除**：卡片池/matchCard/menu/规模重算/即时产卡/预约埋雷/卡片弹窗全链（敌人安排移交正文 AI 自选，态势注入降为信号级驻守提醒）；⑧ 单词条 `副导演`（旧双词条升级下灯不删）；⑨ 报纸新增事件卡（阶段徽标+进度）与风声卡，🌍 世界状态查看器取代 🗂。后续路线图：checkpoint 完整回滚、区域突发事件、账本+远方回响、批量回填。
> **V0.3.1 变更摘要**：主面板 tab 化——报头下栏目导航条（📰 报纸=原版派系征兆流·默认 / ⚡ 事件链 / 📣 风声 三栏分立），`renderWire` 按 `activeTab` 分流渲染，当前态势简报为各 tab 共有头部，activeTab 持久化（ad_ui_v1）。
> **V0.3.2 变更摘要**：真机首推实证修订——① **zoneHit 三通道**（整串包含/压缩通道/切段分段）修复 LLM 真实 zone 写法失配（复合"A至B"/括号注记/无分隔 spot，5 派系 zone 有 4 个漏报、安全区 spot 失效）；② **态势简报几率化**：标题=地点·遇敌几率 N%（最终生效值）/安全区，正文=区域氛围（驻守+爆发警告+副导演 why/heat 理由）；百分比不进正文注入（程序掷骼执行）。
> **V0.3.3 变更摘要**：**S8 checkpoint 完整回滚**——推演写入新世界前快照旧世界到 `$ad_world_checkpoint`，新世界记 `floorId` 锚点；每楼 dispatch 检测楼层回退（`world.floorId > 当前楼层` = 删楼/回退编辑）→ 回滚到上一推演点（本地骰的推进随之丢弃——事件链不会因删楼而越推越快）；单级回滚（锚点重置为当前楼层，继续删楼不再回退）；无更早快照时回到未推演状态；防连战锁随周期重置；swipe 不回滚（楼层号不变，心跳内重推覆盖）。同轮修复 harness mock 两缺陷（replaceVariables 重新赋值致 MOCK.chat 引用脱钩 / eventEmit 包装漏转发参数致 S6 的 swipe/dryRun 守卫失效、真随机 5% 间歇假失败）。

---

## 1. 项目目标与定位

副导演是 AIRP（AI 自由角色扮演）跑团的**世界模拟器**：让世界不等玩家行动也在运转。它与既有两件套构成完整生态：

| 插件 | 职责 | 一句话 |
| :--- | :--- | :--- |
| MiniMapStatus (MMS) | 状态维护（数值/物品/地图/名册） | "我是谁、我在哪、我有什么" |
| RpgCombat | 战斗执行 + 开战初始化（Combat_block 轮询/图鉴载入/导演兜底） | "打的时候怎么算" |
| **AssistantDirector** | 世界模拟（态势/暗线/派系/公开情报） | "世界在背后做什么" |

**要解决的三个痛点**：
1. 正文 LLM 顺从倾向严重——玩家调查几乎不可能失败（→ 叙事阻力注入）；
2. 上下文塞满后正文 LLM 遗忘世界书人物与伏笔（→ 事实提醒注入 + 公开情报流）；
3. 正文 AI 缺乏"世界在背后运转"的参照——敌人在哪、什么戒备、如何反应全靠即兴（→ 态势卡片注入）。

> **已移注边界**：开战初始化全链路（Combat_block 检测/解析/建局、未匹配敌名的导演兜底生成、图鉴写回）已由 RpgCombat V10.12 承担（其 README §10.7）。副导演属地为纯叙事：态势、暗线、派系、公开情报。本插件对战斗数值**零接触**。

---

## 2. 核心设计原则（六条，不可妥协）

1. **程序管数值，AI 管叙事（完全体）**：正文 AI 在整条数值链路上零接触——开战宣告是**点名册**（Combat_block 只写名字），我方数值走 MMS 直连、敌方数值走图鉴/导演兜底（均为 RpgCombat 属地）。
2. **双层信息架构**：玩家只看**公开征兆**（贴边栏：surface），正文 AI 收**幕后真相**（隐藏注入：truth + 渗透方式 + 禁泄清单）。两者成对生成、互不可见对方层，中间的推导距离就是调查空间。玩家无上帝视角选择权。
3. **模拟而非剧本**：不预知玩家行动、不枚举触发器。敌人放置的抽象是**驻防态**（谁/多少/什么戒备/怎么反应），遇敌是语义涌现（正文 AI 判定），不是动作映射。
4. **宣告而非生成**：开战 = 正文 AI 输出最短 `<Combat_block>`（我方/敌方各只写名字）；检测、匹配、展开、建局全是 RpgCombat 程序死活。
5. **开战链路不死锁**：态势注入是**增强层**而非前置条件——未命中卡片池的地点没有菜单也能开战（正文 AI 从图鉴选名 + RpgCombat 兜底链接住）。
6. **地点不可穷尽**（V0.2 硬约束）：AIRP 中地点自由涌现（"悉尼·某后巷·某个苦力的棚屋"），任何依赖预编译地点表、穷举路由的设计不成立。**态势内容的本质是语义判断，归 LLM**（暗线位报告复审 / 态势位即时产卡）；程序只做匹配、配发、校验与触发。MMS 的 18 个 SVG 地图是纯视觉模板，无派系/戒备语义，本插件不引用其作为知识库。

---

## 3. 架构总览（V0.3.0 · 单一副导演 API 世界引擎）

```
┌─ 呈现层（纯程序）──────── 贴边折叠栏悬浮窗：折叠态电报纸带 ticker / 展开态情报流
│                         surface → 玩家（报纸：事件卡/风声卡/派系灰卡揭幕体系）
├─ 本地骰（每楼，零 LLM）──── 事件链阈值骰推进/受挫/晋级（爆发→⚠临近冲突提醒行）
│                         风声安静超时按递增概率消散；lastDiceFloorId 幂等守卫
├─ 随机遭遇掷骰（每楼发送时）─ GENERATION_STARTED 本地掷骰：spots>districts>玩家设置
│                         三级兜底 + heat/eventTension 冷热修正；防连战锁（每周期一场）
└─ 世界推演层（副导演 API · 心跳+强制推）
                          单一 LLM 增量修订 $ad_world（上次状态全量输入）：
                          派系三态暗线（surface/truth/接触/圈套/间谍）+ 关系四字段
                          + 事件链 + 风声 + encounter 遇敌概率档案 + 叙事阻力
                          触发：每 directorEveryX(3) 楼心跳 / 战斗结果·跨日·阶段强制 / 手动📡
```

**数据流闭环**：
```
正文 AI（最强模型）每楼输出：正文 + 状态栏标记点（MMS 属地）+ 条件成立时最短 <Combat_block>
程序（副导演）每楼读 stat_data：本地骰推进事件/风声 → 信号级态势+世界动态注入（单词条）
程序（RpgCombat）轮询 Combat_block → 图鉴载入/导演兜底 → 建局 → 本地结算
$rpg_combat_result（RpgCombat 程序写 MMS）→ 副导演监听 → 强制推演（世界状态反映战果）
```

**可见性矩阵**（双层架构的精确边界）：

| 内容 | 玩家 | 正文 AI |
| :--- | :--- | :--- |
| `factions.surface`（公开征兆） | ✅ 报纸情报卡 + ticker | ❌ 上下文中不存在 |
| `factions.truth` + `resistance` | ❌（调查目标） | ✅ 世界动态注入（三态标注+渗透方式+禁泄） |
| `events`/`winds`（事件链/风声） | ✅ 报纸事件卡/风声卡 | ✅ 世界动态注入（阶段+进度） |
| `encounter`（遇敌概率档案） | ❌（仅 GM 🌍 查看） | ❌ 程序掷骰消费，永不进上下文 |
| 驻守信号（factions.zone 命中） | ✅ 报纸 lead | ✅ 信号级态势注入（无敌人菜单——敌构成正文 AI 自选） |
| 全量世界状态 JSON | GM 面板（主动审计） | ❌ 永不进上下文 |

正文 AI 的上下文里只有**一条**副导演注入（`副导演` 世界书 constant 词条，at_depth/system/深度0/排序15，内容随聊天重写：`<当前态势>` 信号级 + `<内部导演备忘>` 世界动态）；玩家的屏幕上只有一份报纸。

---

## 4. 协议标准

### 4.1 开战宣告通道（`<Combat_block>` 唯一通道）

> 历史注记：短标记 `【战斗标记点丨我方:…丨敌方:…】` 因与状态栏正则/酒馆 markdown 管线冲突过多已废弃（随 RpgCombat 功能回退）。开战宣告唯一通道为 `<Combat_block>`，由 RpgCombat `startSTPolling` 惰性正则检测。

**最短格式**（正文 AI 的全部开战义务 = 一份点名册）：

```yaml
<Combat_block>
战斗数据栏:
  我方列表:
    - 角色:
        名字: "埃利奥特"
    - 角色:
        名字: "弗兰克"
  敌方列表:
    - 角色:
        名字: "伊塔库亚的眷族"
</Combat_block>
```

| 数值环节 | 提供方 | 说明 |
| :--- | :--- | :--- |
| 我方属性/弹药/消耗品 | MMS `stat_data` | RpgCombat V10.11「状态栏传入」直连覆盖 |
| 我方技能 | RpgCombat 持久化 | 聊天变量中玩家点保存的角色数据 |
| 敌方数值 | 图鉴词条 / 导演兜底 | RpgCombat V10.12 世界书载入 + 兜底生成链 |
| 正文 AI | **只写名字** | 可用增强：`*N` 数量后缀、`[变种]` 标签（RpgCombat 原生语义） |

**战斗轮规则**：世界书前部常驻一段简短规则（格式教学/我方名单处理），由用户维护——单一事实源。副导演态势注入**不重复规则全文**，只带提醒（见 §4.5）。

### 4.2 世界状态模型（`$ad_world`，V0.3.0 取代态势卡片池与暗线报告）

**单一状态源**：派系暗线 + 事件链 + 风声 + 遇敌概率档案全部住在一份 JSON 里（聊天变量，随聊天文件走，对 LLM 隐形）。LLM 推演做宏观修订（每 3 楼），本地骰做微观推进（每楼零 LLM）——世界引擎双速模式。

```jsonc
{
  "round": 1,                          // 推演轮次（每次成功推演 +1）
  "digest": "世界现状一句话摘要",
  "factions": [                        // 派系暗线三态（V0.2.x 全保留）+ 关系四字段
    { "name": "凯特·利（萨里山）",
      "surface": "公开征兆（报纸可见，市民视角）",
      "truth": "幕后真相：前文未出现过的深层动机+由动机生长的具体行动",
      "contact": "接触人（姓名/伪装/目的），无则空串",
      "scheme": "圈套（诱饵+杀招），无则空串",        // 恰好 floor(N/2) 个非空
      "mole": "间谍（核心白名单禁选），无则空串",
      "causes": ["楼23"], "state": "推断中|已渗透|已兑现",
      "stance": "对我方立场", "relations": "与其他派系关系",
      "zone": "活动大区（如 萨里山）", "morale": "士气" }
  ],
  "events": [                          // 事件链（本地骰每楼推进）
    { "name": "萨里山炸药交割死局", "type": "conflict|progress",
      "stage": "萌芽|发酵|逼近|爆发|平息", "stageRound": 1-9, "level": 1-4,
      "factions": ["涉及派系"], "zone": "无派系关联时的归属大区", "desc": "一句话现状" }
  ],
  "winds": [                           // 风声（安静超时按概率消散）
    { "content": "一句话风声", "source": "来源", "spread": "私下|流传|公开",
      "level": 1-3, "quietRounds": 0 }   // quietRounds 程序维护，模型不写
  ],
  "encounter": {                       // 遇敌概率档案（S6 掷骰消费，永不进正文上下文）
    "heat": { "value": -30~30, "why": "剧情总体走向冲突为正、缓和为负" },
    "spots":     [ { "match": "地标名", "chance": 0-100, "why": "…" } ],   // chance=0 显式安全区
    "districts": [ { "match": "大区名", "chance": 0-100, "why": "…" } ]
  },
  "resistance": { "forbidden": [...], "partial": [...], "friction": [...] },  // 沿用 V0.2.x
  "roster_ops": [], "generatedAt": 0, "reason": "heartbeat|combat-result|newday|stage-change|manual"
}
```

**本地骰（每楼 dispatchNow 内，零 LLM，仿世界引擎）**：
- **事件链推进**：阈值 = 阶段基准（conflict 越后期越难：85−si×5；progress 反之 75+si×5）− 200×r×(1−r)（进度曲线，中段最难）± level 修正 → 1-100 骰 → 推进（>阈值）/受挫（<阈值×40%，stageRound 回退）/保持；stageRound≥9 晋级下一阶段；conflict 进入"爆发" → 注入"⚠ 临近冲突"提醒行（取代 V0.2.x 预约引爆）；
- **风声衰减**：quietRounds 超 grace(3) 后按 10%+15%/楼 递增概率消散，消散的下报纸架；validateWorld 对延续风声继承 quietRounds（防每次推演重置永不衰减）；
- **幂等守卫**：`State.lastDiceFloorId`——floorId 未前进不掷（swipe/重roll 重触发 dispatchNow 不重复推进）。

**遇敌概率算法**（S6 `encounterProfile` 消费，V0.3.2 定稿）：

```
最终概率 = clamp(0, 100, 区域基值 + 冷热修正)
区域基值 = spots 命中 chance > districts 命中 chance > SETTINGS.randomCombatChance（玩家设置）
冷热修正 = clamp(-40, +40, heat + eventTension)
  heat        = 副导演每轮推演的宏观判断（-30~+30）
  eventTension= 当前地点 conflict 事件阶段分之和（萌芽0/发酵+2/逼近+6/爆发+12/平息−3），
                每楼本地重算——推演间隔内剧情升温概率即时跟随；跨地点事件不计分
显式安全区 = 命中 spots/districts 且 chance===0 → 直接免掷，不叠修正
```

**区块匹配 zoneHit（V0.3.2 三通道，对抗 LLM 真实写法）**：①整串互相包含 ②压缩通道（两边去 `·-—` 空白括号后再包含——"澳大利亚酒店总督套房"↔"澳大利亚酒店 - 总督套房"）③切段通道（zone 按连接词 至/与/、/，/和 切段，每段与地点各分段互相包含，分段 ≥3 字防"悉尼"两字段误报——"悉尼港码头区至禧市排污管网"/"萨里山核心区（绿顶酒馆）"/"达令赫斯特区"等真机实证写法全命中）。驻守信号/爆发提醒/eventTension/spots/districts 全部使用。

**随机遭遇掷骰（S6）**：`GENERATION_STARTED`（用户已发送、提示词未组装）本地掷骰，命中即把强制开战指令（`【🎲随机遭遇】本轮用户触发随机战斗，按【战斗轮规则】输出 Combat_block 块`）以**用户身份**追加进本楼输入末尾——随楼层生成自然持久化，swipe 重roll 指令仍在（防重复标记防二次追加）。豁免：`combatInProgress`（最近可见 AI 楼含 `<Combat_block>`）/ swipe / regenerate / dryRun / 末楼非用户楼 / 安全区。**防连战锁**：命中上锁（`State.randomCombatFired`，持久化）→ 下一次推演成功解锁——每个推演周期最多一场随机战斗；注意锁只约束**随机注入**这一路，剧情开战（正文 AI 自行决定）不触发锁；未首推（无 $ad_world）时锁不生效（周期无边界）。战斗进行中掷骰本身就被 combatInProgress 拦截——锁是"刚打完随机战、正文已收尾但新推演未到"间隙的防护。

### 4.3 世界推演（单一副导演 API）

**触发矩阵**（每楼 dispatchNow 末尾检查；busy 锁防并发）：

| 类型 | 触发条件 | 语义 |
| :--- | :--- | :--- |
| 强制推 | `$rpg_combat_result` 战斗结算变化 | 战果即时反映进世界状态 |
| 强制推 | stat_data 日期跨日（newday） | 晨报：推演昨夜动向 |
| 强制推 | 剧情阶段字段变更 | 阶段剧变，不等心跳 |
| 心跳 | 距上次推演 ≥ `directorEveryX`（默认 3）楼 | 常规节律（用户可调） |
| 手动 | GM 📡 / 🌍 弹窗重推 | 排障 + 玩家主动 |

首楼只记基线不触发（防安装即推演）；换聊天基线重建。V0.2.x 的 city-change 与 floor-cap(15) 已废除（心跳覆盖）。

**输入组装**（信息完整性优先，全量不截断）：世界书同步词条（单套配置）+ LWB 结构化总结 + 最近 N 楼 AI 楼层原文（N=`directorFloors` 默认 20，0=全部）+ 当前时空与敌方动向（分层标注 stat_data）+ 敌方阵营参考（enemyPool）+ 主角核心白名单 + 名册（含墓碑）+ **上次世界状态全量 JSON**（增量修订基线——含本地骰推进结果，模型据此延续未完结事件/未消散风声/三态）+ 附加铁律（用户手输，文末最高优先级）。

**输出校验 validateWorld（宽容模式——只整形不丢弃，杜绝生成却静默丢弃的 token 浪费）**：
- factions：中英键容错映射（真机实证中文键）+ 墓碑派系程序过滤（双保险）+ 三态回落"推断中" + 缺省补默认；
- events：同名事件**继承**上次 stage/stageRound/type（模型忘带时兜底）；非法 type/stage/level 回落；
- winds：同内容风声**继承** quietRounds；非法 spread 回落"流传"、level clamp；
- encounter：heat clamp ±30、chance clamp 0-100、废条目（无 match）剔除；
- resistance：对象条目容错（{target,result}/{source,effect} 拼接，防 [object Object]）。

**暗线人格**（沿袭 V0.2.5）：「开放世界的架构师、顶级权谋小说作家」——反废话铁律、恰好 floor(N/2) 个圈套、动机有根、墓碑禁复活、surface 报纸体；新增铁律：事件"已有具体迹象即可创建"（不要求证据充分）、风声"开始被听闻即可建立"、encounter 对关键地标给 spots（安全据点 chance=0）、没把握的地方不给（程序用玩家设置兜底）。

**提示词预设**（单 DirectorPrompt 链）：默认项锁定不可删（随插件升级实时更新）；新建=当前默认快照，自由修改、固定存储。

### 4.4 派系名册

- **双轨来源**：玩家预输入（MMS 固定名册同构：永远有卡，无事显示"平静"占位，漏更新不丢卡）+ 插件自建（首次出现强制**轻量登场**：仅征兆级，一条传闻/一个名字，不带完整真相——删除成本趋零）。
- **删除 = 墓碑**：级联删该派系全部暗线（交叉暗线整条删，其他派系自身暗线不动），并写废弃清单进战略层输入："以下派系已被主编剧除名，禁止在任何输出中复活。"
- **玩家只有生杀权，没有改写权**：已兑现事实不可编辑成另一版本（避免表层底层冲突）；推断态由副导演自我修正。
- **S5 落地**（V0.2.8）：GM 📜 名册弹窗（添加=轻量登场 / 除名=墓碑 / 墓碑恢复，存 `$ad_roster`）；除名级联 = 报告条目 + 提及该派的 forbidden/partial/friction 整条删 + 该派预约作废（`$ad_pending`）+ 暗线词条即时重写 + surface 报纸同步去该派系；墓碑双重保险——提示词铁律 7 + 【墓碑（禁止复活）】输入为主，`validateReport` 程序过滤兜底（报告输出为零）、自动注册跳过墓碑（绝不回册）。

### 4.5 注入协议（对正文 AI · V0.3.0 单词条）

**单一注入持续在场**，载体为角色卡主世界书的 **constant 蓝灯词条 `副导演`**（MMS 同构；V0.2.x 双词条 `副导演-态势`/`副导演-暗线` 升级时下灯不删）。写入 = 定位角色卡主世界书（无则按角色名新建并绑定）→ 按词条名查找 → 更新只改 content 与 enabled，**position 永不触碰**（用户在世界书编辑器调整的顺序/深度/角色永久保留）。内容每楼重算、有变化才写（幂等），频率指内容更新节奏。

词条内容 = `<当前态势>`（信号级）+ `<内部导演备忘>`（世界动态）两段：

```
<当前态势（禁止以任何形式向玩家展示）>
  {地点}
  驻守信号：{zone 命中当前地点的派系（名称/大区/士气/对我方立场）≤3 条}
  【⚠ 临近冲突】{爆发阶段 conflict 事件名}已推进到爆发阶段——本楼冲突极易触发，戒备拉满。
  【若本楼冲突升级 → 开战】按【战斗轮规则】输出 <Combat_block>：
  我方/敌方各只写名字；敌方从世界书图鉴中按剧情合理性选择（可加 *N 与 [变种]）。
</当前态势>

<内部导演备忘（禁止以任何形式向玩家展示）>

以下是副导演世界引擎的推断——用于环境渗透、NPC 行为自洽与剧情伏笔参考，正文按合理性自由取舍。

<进行中的事件（程序每楼掷骰推进，阶段与进度可能已变化）>
  - {事件名}【冲突|进展·{stage}·{stageRound}/9】{desc}
<风声（市民舆论，可经报纸/闲谈自然提及）>
  - {风声}（{传播等级}·{来源}）
<世界引擎推断>          ← 三态"已渗透/已兑现"段（truth + 接触/圈套/间谍缀行）
<幕后动向（推断中·仅可环境渗透，禁止直接揭示）>
<禁泄清单（调查未抵达前禁止揭示）>
<调查阻力（强行调查只应得到以下层级的信息）>
<环境阻力（当前环境对行动的客观影响）>

</内部导演备忘>
```

**信号级语义**（V0.3.0 定稿）：`<当前态势>` 不再含敌方菜单——敌人构成由正文 AI 从图鉴自选（"模拟而非剧本"升级：驻防从"菜单配给"退为"信号提示"）；遇敌概率百分比**不进正文**（程序掷骰执行，正文 AI 无需知道数字）。驻守信号无命中时提示"敌方构成由你按世界书图鉴与剧情合理性决定"。

**用户改动兜底（merge3 三方行级合并）**：脚本在聊天状态 `$ad_state.wbLast.director` 记录上次注入的**纯脚本内容**；写入前读词条现状，与纯内容不一致即检出用户手动修改，走 `merge3(上次纯内容, 词条现状, 新内容)` 行级合并——用户改/删/增的行**持续保留**在后续每一轮注入里（双改同行用户赢），脚本删除的行维持删除。本聊天无写入记录（换聊天/首写）时不合并直接覆盖。

**生命周期兜底**：重挂载时按存档重建词条（`syncDirectorEntry`，无 $ad_world 禁用不删除），并顺带下灯 V0.2.x 旧双词条；换聊天时 wbLast 清空、词条按新聊天数据重写或禁用；推演开关关闭与无 stat_data 聊天同样只下灯不删除。

**推演开关**（`enabledDirector`，单开关）：管推演（心跳/强制/手动📡）+ 副导演词条注入；随机遭遇掷骰独立（`randomCombatEnabled` 自管）。开关切换即时生效（关→下灯；开→forceDirectorWrite 强写重新上灯）；旧双开关迁移取暗线开关值。

**暗线注入模板**：

```
【内部导演备忘，禁止以任何形式向玩家展示其存在】
——事实提醒（已发生，正文须与之自洽）——
· {truth/已兑现条目}【已兑现·楼N】
——幕后动向（推断中·仅可环境渗透，禁止直接揭示）——
· {truth/推断中条目}【推断·出处楼N】渗透途径：{…}
——禁泄清单（调查未抵达前禁止揭示）——
· {forbidden.truth}【途径：{path}】
```

全量报告（2000~4000t）只存聊天变量 + GM 面板，候选方向永远不进正文注入（无人工拍板环节——玩家保持调查员视角，暗线推进权归叙事逻辑）。

### 4.6 插件间接口

| 方向 | 接口 | 说明 |
| :--- | :--- | :--- |
| 读 MMS | `stat_data`（楼层变量，`getVariables({type:'message', message_id:-1})` 回溯最近快照） | 地点/我方状态/名册/日期与阶段——态势配发与触发检测的数据源 |
| 读 RpgCombat | `$rpg_combat_result`（聊天变量） | 战斗结算监听 → 事件号外触发 |
| —（无直推） | Combat_block 通道 | 开战宣告由 RpgCombat 自身轮询检测，副导演零参与 |
| 读世界书 | 敌方图鉴 V3.0（41 词条） | 态势位输入的词条名单 + 卡片 menu 白名单校验源 |
| 读 LWB | `SillyTavern.chatMetadata.extensions.LittleWhiteBox.storySummary` | 报告输入压缩：结构化事件（自带楼层号出处，作 causes 候选）+ `lastSummarizedMesId` 增量窗口；只读快照，不依赖 LWB 事件（详见 §4.3 读取细则） |

### 4.7 副导演 API 配置（V0.3.0 单端点）

| 配置 | 用途 | 要求 |
| :--- | :--- | :--- |
| `director` | 世界推演（派系/事件链/风声/encounter，每 3 楼一次） | **次高智力**、长上下文、低温（0.3~0.5）、结构遵循优先 |

设置面板沿用 AiRadio `custom_api` 模式（Base URL / Key / Model / 温度 / maxTokens），localStorage 持久化。V0.2.x 双端点迁移：取暗线位配置为 `director`（态势位废弃）；其余设置项——`directorEveryX`（心跳楼数，默认 3）、`directorFloors`（可见楼层窗口）、`enemyPool`（敌方阵营参考）、`coreTeam`（核心白名单）、`extraRules`（附加铁律）、`worldSync`（世界书同步，单套）、`randomCombatEnabled/Chance/OncePerCycle`（随机遭遇三件套）、`debug`。

### 4.8 单 JSON 文件结构（酒馆助手悬浮窗脚本插件）

**外壳**（AiRadio 同构）：

```json
{
  "type": "script",
  "enabled": false,
  "name": "Assistant Director (副导演·世界模拟器)",
  "id": "<uuid>",
  "content": "<UserScript 主体（全部逻辑）>",
  "info": "AIRP 世界模拟器：暗线推演/态势卡片/公开情报贴边栏",
  "button": { "enabled": true, "buttons": [] },
  "data": {},
  "export_with": { "data": true, "button": true }
}
```

**`content`（UserScript）内部分区**（模块化组织，预计 3000~5000 行）：

```
// ==UserScript==（@name/@version/@description/@grant none，AiRadio 头格式）
// ═══ 0. 常量与配置 ═══  版本号 / 默认设置 / 注入模板 / 报警框架首行 / 固定注入 id
// ═══ 1. 设置管理 ═══    localStorage 读写 / 双端点配置（AiRadio custom_api 面板模式）
// ═══ 2. 酒馆环境适配 ═══ 环境检测（脚本环境直用全局 API；降级路径走 window.parent）
//                        API 封装：injectPrompts/uninjectPrompts（持续注入+替换式刷新）
//                        getVariables（message:-1 读 stat_data）/ insertOrAssignVariables（chat 写 $ad_*）
//                        SillyTavern.chatMetadata（读 LWB storySummary 快照）
//                        eventOn（tavern_events.*）/ getChatMessages / 世界书读取
// ═══ 3. 楼层监听状态机 ═══ MESSAGE_RECEIVED/UPDATED/SWIPED/CHAT_CHANGED
//                        swipe 去抖 / 跨日检测→报告触发 / 地点变更检测→产卡触发
//                        $rpg_combat_result 监听→号外 / 阶段字段变更→号外
// ═══ 4. 态势卡片引擎 ═══  卡片池 CRUD / 地点三级匹配 / 规模随行重算 / 注入拼装
//                        通用兜底 / 待登记队列 / 预约条件检查与引爆 / menu 白名单校验
// ═══ 5. LLM 客户端 ═══   双端点 fetch（/chat/completions 非流式）/ JSON 提取容错 / 重试
// ═══ 6. 战略层（暗线人格）═══ 输入组装（LWB 摘要+增量窗口原文+分层 stat_data+名册+墓碑+待登记）
//                        schema 硬校验（causes/truth-surface 成对/三态/白名单）
//                        报告存档 $ad_report / 提炼注入生成 / 三路分发
// ═══ 7. 态势位（即时产卡）═══ 输入组装（地点+正文尾部+图鉴名单+名册+阶段）
//                        单卡/多卡生成 / 校验 / 失败回落通用注入
// ═══ 8. 公开层 UI ═══    贴边折叠栏悬浮窗（document.body.appendChild + position:fixed）
//                        折叠 ticker / 展开报纸面板 / 派系卡 / 灰卡解锁 / 未读脉冲
// ═══ 9. GM 面板 ═══      报告存档查看 / 手动「立即推演」/ 名册 CRUD / 墓碑
//                        注入审计（每条 truth 回溯 causes 楼层）
// ═══ 10. 主流程编排 ═══   初始化序列 / 事件接线 / 全局容错兜底
```

**存储分层**：

| 数据 | 存储 | 说明 |
| :--- | :--- | :--- |
| 端点/参数/UI 偏好 | localStorage（`ad_*` 键） | AiRadio 模式，随浏览器走 |
| 卡片池 | 聊天变量 `$ad_cards` | `$` 前缀对 LLM 隐形，随聊天文件走 |
| 运行时状态 | 聊天变量 `$ad_state` | 弹药基准（规模降档参照，历史最高）/ 上次注入文本（幂等去重）/ 上次地点 |
| 最新报告 | 聊天变量 `$ad_report` | GM 面板与分发源 |
| 名册 + 墓碑 | 聊天变量 `$ad_roster` | 双轨 CRUD |
| 待登记 + 预约 | 聊天变量 `$ad_pending` | 即时轨/引爆消费 |
| 注入 | 固定 id `ad_situation` / `ad_shadowline` | 替换式：先 uninject 旧 id 再 inject 新内容 |

---

## 5. 功能边界

**In Scope**：
- 单 JSON 悬浮窗插件（外壳 + UserScript 全逻辑）
- 贴边折叠栏 UI（折叠 ticker / 派系情报卡 / 阶段揭示 / 未读脉冲）
- 态势卡片引擎（卡片池/三级匹配/规模重算/通用兜底/待登记闭环）
- 态势位即时产卡（随地点触发、分兵多卡、校验回落）
- 暗线人格（触发矩阵、schema 校验、三态生命周期、提炼注入、报告存档）
- 派系名册（双轨、墓碑、轻量登场）
- 预约-引爆（结构化条件每楼检查）
- GM 面板（报告查看/手动推演/名册管理/注入审计）

**Out of Scope**：
- 不做玩家上帝视角的选择面板（GM 拍板已被否决——调查员视角纯粹性）
- 不做数值修改/战斗结算/开战建局/导演兜底（全部 RpgCombat 属地）
- 不做状态维护（MMS 属地）
- 不依赖固定触发词表或预编译地点表（剧本思维与穷尽假设均被否决）
- 不做正则替换的内联楼层组件（悬浮窗形态，不与同楼层组件共享文本）

---

## 6. 幻觉防线（四道闸，按强度排序）

1. **出处强制**：`causes` 必填真实楼层引用，无出处条目不允许进真相层（程序校验）；
2. **三态隔离**：推断永远标"推断"，错不污染事实层；只有正文实际演出过才升级；
3. **输入完整性 + 白名单**（V0.2.2 修订）：**信息完整性优先于上下文预算**——长上下文模型时代，缺信息导致的瞎编比长输入的稀释更危险。世界书同步词条与正文上下文全量注入；白名单校验不变（态势 menu ∈ 图鉴、causes 楼层出处强制）；
4. 温度 0.3~0.5（辅助）。

---

## 7. UI 视觉规范

视觉 demo 已完成：`demo_story_director.html`（项目根目录，零依赖）。悬浮窗实现为 `document.body.appendChild` + `position:fixed` 贴边（AiRadio 同构），非楼层内 iframe。

- **折叠态**：右侧贴边 40px 竖条，竖排"世界情报 ✦ GAZETTE"标签 + 竖向滚动未读头条（电报机纸带隐喻）+ 未读琥珀金脉冲点；无动态时静默——"是否留意时事"是玩家决策。
- **展开态**：右缘滑出 372px 报纸增刊面板：报头（日期+剧情阶段）→ 派系情报卡×N（每条情报=一句话动态+来源+日期）→ 底注"本报仅刊载街头可见之事 · 真相须由读者自行抵达"。
- **派系卡**（S4 完全体，V0.2.9）：**接触即揭**——派系名（或去括号核心名）出现在玩家可见视野（非隐藏楼层原文 + LWB 总结 + 当前敌人名单）任一即署名，`$ad_roster.revealed` 单调只增不减（旧楼被 LWB 隐藏/敌人名单轮换后不回退灰卡）；未接触派系灰态"？？？"遮名（只可见征兆；不出占位卡——不剧透存在感，未接触派系只经征兆灰卡登场）；插件自动登记打"新面孔"标记（📜 预输入无）；新揭示首拍金色揭幕闪动；已接触但本轮报告无条目的名册派系给"平静"占位卡（漏更新不丢卡）；墓碑除名后条目/征兆/占位即刻全清（不再复活）。
- **视觉基因**：MMS slate 深底（#0f172a）+ 浅灰蓝文字（#cbd5e1）+ 琥珀金（#fbbf24）+ Courier 打字机字体 + 1920s 报纸双线框/竖分栏线；与 `regex-1920s新一天新闻与账单美化.json` 美学同源。

---

## 8. 分步实现计划（每步可测）

> S0~S6 为 V0.1~V0.2.x 时代的原始计划（卡片池双轨架构），S7 起为 V0.3.x 世界引擎架构——原 S1 卡片引擎/S2 即时产卡/S3 暗线报告已于 V0.3.0 被世界状态模型取代拆除。

| Step | 内容 | 状态 |
| :--- | :--- | :--- |
| **S0 骨架** | 单 JSON 外壳 + 悬浮窗贴边折叠栏 UI + 设置面板 | ✅ V0.1 |
| **S1 卡片引擎** | 三级匹配/规模重算/态势注入 | ✅ 已被 S7 取代 |
| **S2 态势位** | 即时产卡全链路 | ✅ 已被 S7 取代 |
| **S3 战略层** | 触发矩阵/报告校验/提炼注入 | ✅ 演化为 S7 推演层 |
| **S4 公开层** | surface 派系卡（灰卡/揭幕/墓碑） | ✅ V0.2.9 |
| **S5 名册** | 名册 CRUD / 墓碑级联 | ✅ V0.2.8（预约引爆已被事件链"爆发"阶段取代） |
| **S6 随机遭遇** | GENERATION_STARTED 掷骰注入用户楼 | ✅ V0.3.0 |
| **S7 世界引擎** | 单一副导演 API / $ad_world / 本地骰 / 遇敌概率档案 / 防连战锁 / 心跳+强制推 / 单词条 / tab 化 / zoneHit | ✅ V0.3.0~0.3.2 |
| **S8 checkpoint 回滚** | 推演前快照 $ad_world，删楼/楼层回退时回滚到上一推演点 | ✅ V0.3.3 |
| **后续路线图** | 区域突发事件（3% 权重轮盘）/ 账本+远方回响 / 批量回填 / 时间模式节奏 / worldSync 蓝绿灯 | 规划中 |

---

## 9. 验收与质量标准

1. **兼容性**：单 JSON 在 Chrome/Edge 酒馆助手环境零控制台报错；悬浮窗降级条件（页面不可见/失焦）下逻辑兜底正常（IAB 验证）。
2. **数值零幻觉**：正文 AI 的任何输出不直接进入数值链路；副导演对战斗数值零接触；态势 menu 全程白名单可追溯（日志）。
3. **忠实性**：暗线注入的每条真相可回溯到 causes 楼层出处（GM 面板审计接口）。
4. **稳定度**：连续 50 楼监听无内存泄漏、注入不堆积（固定 id 替换式验证）；swipe 不误触发报告/产卡。
5. **成本纪律**：每楼零 LLM 调用；态势位平均 ≤1 次/3 楼；暗线位天级 + 号外。
6. **开发流程**：遵循 sillytavern-plugin-dev 分流——编码后提取 content 做 `node --check` → IAB mock 测试 → 用户导入真环境确认；布局动画类最终过真 Chrome（排障模式）。