# Assistant Director（副导演）项目设计规范文档

**版本：V0.1（立项）** ｜ 单文件形态：`index.html`（酒馆助手 TavernHelper iframe 插件）

---

## 1. 项目目标与定位

副导演是 AIRP（AI 自由角色扮演）跑团的**世界模拟器**：让世界不等玩家行动也在运转。它与既有两件套构成完整生态：

| 插件 | 职责 | 一句话 |
| :--- | :--- | :--- |
| MiniMapStatus (MMS) | 状态维护（数值/物品/地图/名册） | "我是谁、我在哪、我有什么" |
| RpgCombat | 战斗执行（纯本地数值引擎） | "打的时候怎么算" |
| **AssistantDirector** | 世界模拟（暗线/敌人放置/战斗初始化） | "世界在背后做什么" |

**要解决的三个痛点**：
1. 正文 LLM 顺从倾向严重——玩家调查几乎不可能失败（→ 叙事阻力注入）；
2. 上下文塞满后正文 LLM 遗忘世界书人物与伏笔（→ 事实提醒注入 + 公开情报流）；
3. 敌人放置/开战的数值工作靠提示词约束 LLM 不可靠（→ 标记点协议 + 程序组装）。

---

## 2. 核心设计原则（不可妥协的五条）

1. **程序管数值，AI 管叙事（完全体）**：正文 AI 在整条数值链路（我方数值/敌方数值/结算回写）上零接触。它输出的每个动作都是"选择题"（从注入池里选名字），不是"填空题"（凭空写数值）。
2. **双层信息架构**：玩家只看**公开征兆**（贴边栏：表面信息），正文 AI 收**幕后真相**（隐藏注入）。两者成对生成，中间的推导距离就是调查空间。玩家无上帝视角选择权。
3. **模拟而非剧本**：不预知玩家行动、不枚举触发器。敌人放置的抽象是**驻防态**（谁/多少/什么戒备/怎么反应），遇敌是语义涌现（正文 AI 判定），不是动作映射。
4. **宣告而非生成**：开战 = 正文 AI 打一个 ~30 token 的标记；组装、匹配、展开、注入全是程序死活。
5. **开战链路不死锁**：三级容错（菜单命中 → 修补生成 → 图鉴近似兜底），任何一级失败都有下家。

---

## 3. 架构总览（四层）

```
┌─ 呈现层（实时）──────── 贴边折叠栏：折叠态电报纸带 ticker / 展开态派系情报卡
│                         公开征兆 → 玩家 + 单向流向 MMS 行动选项
├─ 战斗适配层（每楼，纯程序）─ 态势计算：场景类型 × 派系控制 × 戒备等级
│                         → 亮菜单（本场景可选敌方词条+规模区间）+ 判定标准
│                         → 深度0 system 隐藏注入；规模随行重算（读 stat_data）
│                         标记点检测 → 解析 → 组装 Combat_block → 直推战斗前端
├─ 战略层（10轮+事件，LLM）─ 暗线人格：派系动机/地盘与戒备修正/来袭预约
│                         低频慢思考埋雷；暗线三态生命周期；报告存档+提炼注入
└─ 修补层（按需，LLM）──── 战斗修补人格：未匹配敌名 → 生成指南驱动的新词条
                          程序校验（标签白名单/MP/Aim/格式）→ 动态图鉴沉淀
```

**数据流闭环**：
```
正文 AI（最强模型）每楼输出：正文 + 【状态栏标记点】(~10t) + 条件成立时【战斗标记点】(~30t)
  → 程序组装（敌方=态势池词条，我方=状态栏直连）→ 直推 RpgCombat 建局
  → 战斗纯本地结算 → $rpg_combat_result 程序写 MMS
  → 副导演监听结算事件 → 事件快报（暗线推进 + 戒备调整 + 阶段判断）
```

---

## 4. 协议标准

### 4.1 战斗标记点语法

```
【战斗标记点丨我方:埃利奥特/弗兰克丨敌方:沙蝎信徒*3/看门狗*1[荆棘]】
```

| 段 | 必填 | 语义 |
| :--- | :--- | :--- |
| `我方:名字/名字` | 可选 | 在场宣告；省略 = MMS 全员存活者默认参战；未列出者不进战斗 |
| `敌方:名字*N/名字[变种]` | 必填 | 敌方单位与数量；`*N` 复数后缀、`[标签]` 变种标签（双写入点语义，战斗前端原生支持） |
| `场地:场地名` | 预留 | 映射战斗前端场地机制 |
| `特殊:伏击` | 预留 | 走副导演预约的来袭池而非当前态势池 |

**解析规则**：宽松容错（全角/半角冒号与竖线、空格、漏段）；我方名走 MMS `stat_data` 匹配（整词、剥 emoji，复用开局直连逻辑）；敌方名走图鉴匹配（剥 emoji 精确 → 别名 → 双向包含，复用 RpgCombat `resolveWorldbookEnemy` 三级匹配语义）。

**失败行为**：我方未匹配 → 备战界面黄标上报（不静默编造）；敌方未匹配 → 进入修补层（§4.6）；彻底解析不动 → 提示玩家手动开战（RpgCombat 备战界面支持粘 YAML）。

**写回路径**：程序将组装好的 `<Combat_block>` 通过聊天变量/iframe 事件**直推** RpgCombat 建局入口（`loadManualData`/`onCombatDataReceived` 语义）；DOM 上标记渲染为"⚔️ 战斗已就绪"备战卡片（iframe，MMS 同款正则替换模式）。**原始楼层文本零修改**（swipe/删楼/分支天然兼容）。

### 4.2 态势模型

```
态势 = 场景类型模板 × 派系控制权 × 戒备等级
```

- **场景类型**：对齐 MMS 小地图的 18 个通用 SVG 模板（【酒店】【旅馆】【杂货店】…类型级，非实例级）。每类型带反应模式模板（如商铺类：白天盘问放行 / 夜间喝止不听即动手 / 持械直接敌对）。
- **派系控制权**：哪个派系控制哪个场景实例——战略层维护 + 玩家名册预登记。实例锚点 = MMS `地点` 自由文本字段。
- **戒备等级**：`松懈/常规/警戒/严密` 四档，三处来源可改——战略层修正（覆盖默认）、玩家行为标记（"世界记得你"：上楼被盘问/打听 → +1）、预约引爆（置顶格）。
- **路由分档**：精路由（18 场景类型模板）+ 粗路由（阶段 × 地形 × 距主线距离，兜底未知地点）+ 新地点自动登记（玩家停留的新地点经 MMS 小地图自由字段登记，下次精路由）。
- **规模随行重算**：每楼读最新 `stat_data`（HP/弹药/倒地）按公式重算"若遇敌的敌方池与规模"，遇敌楼即取即用。规模参考：普通=1级×4~6 或 2级×2~3；硬仗=3级×2+2级×2；头目=4级×1+2~3级随从；终局=5级×1+精英+杂兵；我方有倒地或弹药过半 → 数量-1~2 或降一级。

**态势注入格式**（每楼，深度0 system，隐藏）：

```
【当前态势 · {地点}】
驻守：{派系} {词条名}（{等级}级×{数量}），戒备等级：{档位}
反应模式：{场景类型模板描述}
【本场景可选敌方词条】{名字}*{min~max} / … /（粗路由兜底：…）
【若本楼冲突升级 → 开战】输出【战斗标记点丨敌方:…】；判定标准：玩家行动在
语义上构成对上述戒备的挑衅/侵入/暴露时开战或转入对峙；未构成则正常叙事。
禁止自创敌方数值，只能从上方词条中选择。
```

### 4.3 暗线数据结构（战略层输出 schema）

```jsonc
{
  "stage": "阶段判断 + 依据楼层",
  "factions": [                       // 派系动态
    { "name": "沙蝎教团", "surface": "公开征兆（进贴边栏）",
      "truth": "幕后真相（仅进隐藏注入）", "causes": ["楼12 蒂莉透露账目"],
      "state": "推断中|已渗透|已兑现" }
  ],
  "resistance": {                     // 叙事阻力（治顺从性）
    "forbidden": [ { "truth": "禁泄真相", "path": "正确获取途径", "leak_cost": "过早泄露毁掉什么" } ],
    "partial": [ "强行调查应得的部分信息/误导" ],
    "friction": [ "来自已登场势力动机的环境阻力" ]
  },
  "roster_ops": [ "新派系提案（轻量登场：仅征兆级）" ],
  "garrison_overrides": [ { "场景/地点": "…", "修正": "戒备/池变更" } ],
  "ambush预约": [ { "派系": "…", "条件": "时间/地点/规模", "引爆态": "戒备顶格" } ]
}
```

**硬校验**（程序侧，不过即丢弃/重试）：
- `causes` 必填且必须引用真实楼层号——**"已发生"的判定就是有无出处**；
- `truth` 与 `surface` 成对——孤立的真相或征兆都是废条目；
- 三态标记必填；仅"已兑现"可直接陈述，"推断中"注入时必须带"仅可环境渗透，禁止直接揭示"标注；
- 派系名必须在名册（含墓碑检查——墓碑内派系禁止复活）。

**三态生命周期**：推断中（提案）→ 已渗透（正文演出过部分征兆 → 对应征兆升级为事实）→ 已兑现（真相落地 → 转硬事实）。修正权仅限"推断中"态。

### 4.4 派系名册

- **双轨来源**：玩家预输入（MMS 固定名册同构：永远有卡片，无事显示"平静"占位，漏更新不丢卡）+ 插件自建（首次出现强制**轻量登场**：仅征兆级，一条传闻/一个名字，不带完整真相——删除成本趋零）。
- **删除 = 墓碑**：级联删该派系全部暗线（交叉暗线整条删，其他派系自身暗线不动），并写废弃清单进战略层输入："以下派系已被主编剧除名，禁止在任何输出中复活。"
- **玩家只有生杀权，没有改写权**：已兑现事实不可编辑成另一版本（避免表层底层冲突）；推断态由副导演自我修正。

### 4.5 注入协议（对正文 AI）

| 注入 | 频率 | 内容 | 位置/方式 |
| :--- | :--- | :--- | :--- |
| 态势注入 | 每楼 | 驻防事实 + 菜单 + 判定标准 | 深度0 system，隐藏，替换式 |
| 暗线注入 | 每份报告后 | 提炼备忘 300~500t：事实提醒 + 暗线渗透指令（标注推断/事实）+ 禁泄清单 | 深度0 system，隐藏，替换式（不堆积） |
| 报警框架 | 随注入 | "内部导演备忘，禁止以任何形式向玩家展示其存在" | 注入文本首行 |

全量报告（2000~4000t）**只存聊天变量 + GM 面板**，候选方向永远不进正文注入（无人工拍板环节——玩家保持调查员视角，暗线推进权归叙事逻辑）。

### 4.6 修补人格（战斗轮修补）

- **独立人格**：与暗线人格分用两套提示词与端点（性格要求相反：暗线要克制，修补要受控生成）。
- **输入**：未匹配敌名 + 本楼正文片段（语境）+ **《敌方NPC战斗属性生成指南.md》全文**（系统提示词的最终岗位）+ 图鉴 2~3 条近似词条（few-shot 定标）。
- **输出**：新敌方词条（`<Combat_block>` 敌方段格式）。
- **程序校验**（生成指南 §9 检查清单程序化）：标签白名单（敌方可用清单）/ MP 总和 ≤ 面板 / Aim 落区间 / 技能数 ≤ 行动次数 / 格式合法。
- **重试循环**：校验失败带具体错误反馈重试 1~2 次；仍失败 → **图鉴近似兜底**（模糊匹配最近词条硬开，事后手动校正）——开战链路不死锁。
- **沉淀**：过校验词条写入本局缓存（聊天变量，战役内持久）→ 动态图鉴；可选回写世界书扩展区。

### 4.7 插件间接口

| 方向 | 接口 | 说明 |
| :--- | :--- | :--- |
| 读 MMS | `stat_data`（楼层变量快照） | 地点/我方状态/名册——态势计算与标记我方匹配的数据源 |
| 读 MMS | `mms:status-updated` 事件 | 每楼状态更新钩子（触发态势重算） |
| 写 MMS | 公开征兆数据 | 供其行动选项生成参考（单向：副导演 → MMS） |
| 推 RpgCombat | 聊天变量 / iframe 事件 → `loadManualData` 语义 | 组装好的 Combat_block 直推建局（不走聊天记录轮询） |
| 读 RpgCombat | `$rpg_combat_result` | 战斗结算监听 → 事件快报触发 |
| 读世界书 | 敌方图鉴 V3.0（41 条词条） | 敌方词条源 + 派系池；含动态扩展区（聊天变量） |

### 4.8 双模型端点配置

| 模型位 | 用途 | 要求 |
| :--- | :--- | :--- |
| 暗线位 | 战略层报告 | 长上下文、便宜、低温（0.3~0.5） |
| 修补位 | 战斗轮修补 | 次高智力、结构遵循优先、中低温 |

设置面板沿用 AiRadio `custom_api` 模式（Base URL / Key / Model / 参数），两套独立存储与持久化。

---

## 5. 功能边界

**In Scope**：
- 贴边折叠栏 UI（折叠 ticker / 派系情报卡 / 阶段揭示 / 未读脉冲）
- 标记点解析、词条匹配、Combat_block 组装与直推
- 态势计算（精/粗路由、戒备动态、规模随行重算）与每楼注入
- 暗线人格（双触发、schema 校验、三态生命周期、提炼注入）
- 修补人格（校验循环、图鉴近似兜底、动态图鉴沉淀）
- 派系名册（双轨、墓碑、轻量登场）
- 预约-引爆（来袭预约条件检查与引爆）

**Out of Scope**：
- 不做玩家上帝视角的选择面板（GM 拍板已被否决——调查员视角纯粹性）
- 不做数值修改/战斗结算（RpgCombat 属地）
- 不做状态维护（MMS 属地）
- 不依赖固定触发词表（剧本思维已被否决——驻防态模拟）
- 不枚举全部地图地点（精/粗路由 + 自动登记替代）

---

## 6. 幻觉防线（四道闸，按强度排序）

1. **出处强制**：`causes` 必填真实楼层引用，无出处条目不允许进真相层（程序校验）；
2. **三态隔离**：推断永远标"推断"，错不污染事实层；只有正文实际演出过才升级；
3. **输入压缩**：战略层输入 = 剧情压缩摘要 + 最近 2~3 轮原文 + 分层标注 stat_data（硬事实）——输入越短出处越准；
4. 温度 0.3~0.5（第四道闸，辅助）。

---

## 7. UI 视觉规范

视觉 demo 已完成：`D:\Project\MiniMapStatus\demo_story_director.html`（15.7KB，零依赖）。

- **折叠态**：右侧贴边 40px 竖条，竖排"世界情报 ✦ GAZETTE"标签 + 竖向滚动未读头条（电报机纸带隐喻）+ 未读琥珀金脉冲点；无动态时静默——"是否留意时事"是玩家决策。
- **展开态**：右缘滑出 372px 报纸增刊面板：报头（日期+剧情阶段）→ 派系情报卡×N（每条情报=一句话动态+来源+日期）→ 底注"本报仅刊载街头可见之事 · 真相须由读者自行抵达"。
- **派系卡**：预输入派系永远有卡（平静占位）；插件新建打"新面孔"标记；未接触派系灰态"？？？"（阶段揭示节奏器）；阶段推进时灰卡解锁带金色揭幕闪动。
- **视觉基因**：MMS slate 深底（#0f172a）+ 浅灰蓝文字（#cbd5e1）+ 琥珀金（#fbbf24）+ Courier 打字机字体 + 1920s 报纸双线框/竖分栏线；与 `regex-1920s新一天新闻与账单美化.json` 美学同源。

---

## 8. 分步实现计划（每步可测）

| Step | 内容 | 验收测试 |
| :--- | :--- | :--- |
| **S0 骨架** | 单 HTML + TavernHelper iframe 挂载 + 贴边折叠栏 UI（自 demo 移植）+ 设置面板（双模型端点） | IAB 打开：折叠/展开缓动、ticker 循环、设置持久化（localStorage→聊天变量） |
| **S1 标记解析器** | 标记语法解析（宽松容错）+ 名单解析（*N/[标签]/可选我方）+ 图鉴三级匹配 + stat_data 我方匹配 + Combat_block 组装 | **纯函数单测**：喂标记字符串 → 断言解析 AST 与组装 YAML（含容错用例：全角半角/空格/漏段） |
| **S2 标记检测与建局** | 楼层渲染后检测标记 → DOM 备战卡片（MMS 正则替换模式）→ 变量/事件直推 RpgCombat 建局 | 真酒馆：正文楼层贴标记 → 战斗前端建局，我方数值来自状态栏；未匹配我方黄标 |
| **S3 修补人格** | 未命中敌名 → 修补端点调用（指南注入）→ 词条校验（白名单/MP/Aim/格式）→ 重试循环 → 图鉴近似兜底 → 词条沉淀 | 喂未知名字 → 生成过校验建局；构造校验失败样本 → 断言重试与兜底路径 |
| **S4 名册与态势模型** | 名册 CRUD（双轨/墓碑/轻量登场）+ 态势数据结构（场景×派系×戒备）+ 18 场景类型模板 + 粗路由 | 配置名册 → 变量持久化 → 态势查询断言（场景×派系组合 → 正确驻防描述） |
| **S5 态势注入** | 每楼读 stat_data → 态势计算 → 规模随行重算 → 菜单+判定标准注入 | 换地点楼层 → 注入内容变化断言；弹药减半 → 规模降档断言 |
| **S6 暗线人格** | 双触发（10轮+事件）→ 输入组装（压缩+分层+墓碑）→ 暗线端点 → schema 校验 → 报告存档 → 提炼注入 | mock 十轮 → 报告过校验 → 注入含分类标注；构造无 causes 条目 → 断言被拒 |
| **S7 公开层实装** | 征兆 → 派系卡渲染（阶段揭示/新面孔/灰卡解锁）→ 事件快报刷新 → 公开层单向输出 MMS | 报告生成后贴边栏卡片更新；阶段推进解锁灰卡；删除派系墓碑生效（不再复活） |
| **S8 预约-引爆 + 叙事阻力** | 来袭预约条件每楼检查 → 引爆（戒备置顶+态势标注）；禁泄清单进正文注入 | 埋预约 → 条件满足楼断言注入含主动接触态 |
| **S9 集成回归** | 三插件全链路（真模型正文打标记→建局→战斗→结算→快报）+ harness（参照 RpgCombat integration-test 模式，mock 酒馆环境） | 端到端断言套件全绿 |

**阶段分组**：S0–S2 = 标记闭环（最小可用）；S3 = 容错补全；S4–S5 = 态势就绪；S6–S7 = 战略层上线；S8–S9 = 完全体。

---

## 9. 验收与质量标准

1. **兼容性**：单 HTML 在 Chrome/Edge 下零控制台报错；iframe 降级条件（不可见/失焦）下逻辑兜底正常（IAB 验证）。
2. **数值零幻觉**：正文 AI 的任何输出不直接进入数值链路；标记解析→组装全程程序可追溯（日志）。
3. **开战不死锁**：三级容错全路径有测试覆盖。
4. **忠实性**：暗线注入的每条真相可回溯到 causes 楼层出处（审计接口）。
5. **稳定度**：连续 50 楼监听无内存泄漏、注入不堆积（替换式验证）。
6. **开发流程**：遵循 sillytavern-plugin-dev 分流——编码后 `node --check` 语法校验 → IAB mock 测试 → 用户导入真环境确认；布局动画类最终过真 Chrome（排障模式）。
